---
phase: 01-core-api-type-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/react/lib/Particles.tsx
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-05
must_haves:
  truths:
    - "User can mount `<Particles />` and see rendering using the documented component contract."
    - "User can configure particles via `options` or `url` and both paths are respected."
    - "User can pass `id`, `width`, `height`, `style`, and `className` without breaking render behavior."
  artifacts:
    - path: "components/react/lib/Particles.tsx"
      provides: "Runtime React wrapper that maps props to tsParticles.load lifecycle"
  key_links:
    - from: "components/react/lib/Particles.tsx"
      to: "@tsparticles/engine"
      via: "tsParticles.load({ id, options, url })"
      pattern: "tsParticles\.load\(\{ id, options, url \}\)"
    - from: "components/react/lib/Particles.tsx"
      to: "DOM container"
      via: "rendered div props"
      pattern: "<div id=\{id\} className=\{className\} style=\{style\}"
---

<objective>
Harden the `Particles` runtime wrapper behavior while preserving the stable public API and dual configuration paths.

Purpose: Deliver CORE rendering/config prop truths with additive modernization (D-05, D-06, D-09, D-10).
Output: Updated `Particles.tsx` with explicit, consistent lifecycle and prop passthrough behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-api-type-contracts/01-CONTEXT.md
@components/react/lib/Particles.tsx
@components/react/lib/IParticlesProps.ts
@components/react/lib/ParticlesProvider.tsx

<interfaces>
From `components/react/lib/Particles.tsx`:
```typescript
const Particles: FC<IParticlesProps> = ({
  id: idProp,
  url,
  options,
  className,
  style,
  particlesInit,
  particlesLoaded,
}) => { ... }
```

```typescript
const container = await tsParticles.load({ id, options, url });
return <div id={id} className={className} style={style}></div>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve dual config loading and lifecycle guards</name>
  <files>components/react/lib/Particles.tsx</files>
  <read_first>
    - components/react/lib/Particles.tsx
    - components/react/lib/IParticlesProps.ts
    - .planning/phases/01-core-api-type-contracts/01-CONTEXT.md
  </read_first>
  <behavior>
    - `<Particles />` uses provided `options` or `url` inputs in load call (CORE-02/CORE-03).
    - unmount cleanup always destroys the current container without leaking references.
  </behavior>
  <action>Refactor `Particles.tsx` internals to modern, consistent React/TypeScript style (per D-09) while preserving current behavior: keep async dynamic import, `tsParticles.load({ id, options, url })`, mounted/unmounted guarding, and cleanup destruction pattern (per D-10). Do not remove either config path or callback hooks (per D-05/D-06).</action>
  <acceptance_criteria>
    - `components/react/lib/Particles.tsx` contains `tsParticles.load({ id, options, url })`.
    - Cleanup block still contains `containerRef.current?.destroy()` and resets reference.
    - Effect dependency list includes `id`, `options`, `url`, `particlesInit`, and `particlesLoaded`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @tsparticles/react build:ci</automated>
  </verify>
  <done>Runtime flow remains stable for both options and URL modes with explicit cleanup behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Guarantee common render prop passthrough</name>
  <files>components/react/lib/Particles.tsx</files>
  <read_first>
    - components/react/lib/Particles.tsx
    - components/react/lib/IParticlesProps.ts
    - .planning/ROADMAP.md
  </read_first>
  <action>Ensure render passthrough for `id`, `className`, and `style` remains explicit and unchanged (CORE-05), and retain support for width/height through style-driven CSSProperties usage as part of the existing contract. Keep default id fallback behavior stable (`tsparticles`) for backward compatibility (D-06).</action>
  <acceptance_criteria>
    - Return JSX includes `id={id}`, `className={className}`, and `style={style}` on the rendered container.
    - The id fallback remains `idProp ?? "tsparticles"`.
    - No prop removals from the `IParticlesProps`-driven component signature.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @tsparticles/react build:ci</automated>
  </verify>
  <done>Users can pass documented render props without breaking component mount/render behavior.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                               |
| ------------------------------------- | ----------------------------------------------------------------------------------------- |
| React props → particle runtime loader | Untrusted consumer props (`id`, `options`, `url`, callbacks) cross into engine load calls |

## STRIDE Threat Register

| Threat ID | Category | Component                          | Disposition | Mitigation Plan                                                                                                               |
| --------- | -------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-01-04   | T        | components/react/lib/Particles.tsx | mitigate    | Preserve explicit prop-to-load mapping (`id`, `options`, `url`) and avoid implicit mutation/derivation of user input.         |
| T-01-05   | D        | components/react/lib/Particles.tsx | mitigate    | Keep deterministic cleanup (`destroy` + ref reset) on unmount to reduce leaked runtime instances.                             |
| T-01-06   | E        | components/react/lib/Particles.tsx | accept      | URL-origin policy hardening is out of Phase 1 scope; maintain existing behavior and defer stricter policy gates to Phase 2/3. |

</threat_model>

<verification>
Run package compile/build checks:
- `pnpm --filter @tsparticles/react build:ci`
</verification>

<success_criteria>

- `CORE-01`, `CORE-02`, `CORE-03`, and `CORE-05` behaviors remain observable in component code path.
- No breaking API removals from `Particles` props.
- Lifecycle cleanup remains explicit and stable.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-api-type-contracts/01-core-api-type-contracts-02-SUMMARY.md`
</output>
