---
phase: 01-core-api-type-contracts
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - components/react/package.json
  - apps/react/package.json
  - apps/react-vite/package.json
  - apps/nextjs/package.json
  - apps/nextjs-beta/package.json
  - templates/react/package.json
  - templates/react-ts/package.json
autonomous: true
requirements:
  - CORE-01
  - CORE-04
  - TYPE-01
must_haves:
  truths:
    - "Phase 1 package compatibility policy remains broad for existing consumers while targeting engine beta line."
    - "Monorepo consumers align on the same engine major line as the core package after updates."
  artifacts:
    - path: "components/react/package.json"
      provides: "Library peer/dependency contract for React + engine"
    - path: "apps/*/package.json"
      provides: "Consumer compatibility smoke matrix dependency alignment"
    - path: "templates/*/package.json"
      provides: "Starter-template dependency alignment with package contract"
  key_links:
    - from: "components/react/package.json"
      to: "apps/*/package.json"
      via: "shared @tsparticles/engine major line"
      pattern: '"@tsparticles/engine"'
    - from: "components/react/package.json"
      to: "templates/*/package.json"
      via: "shared package contract expectations"
      pattern: '"@tsparticles/react"'
---

<objective>
Align package/dependency metadata across library, apps, and templates to match the Phase 1 compatibility direction without narrowing consumer React support.

Purpose: Implement D-01, D-02, D-03, D-11, and D-12 via coordinated monorepo contract updates.
Output: Updated package manifests and compatibility smoke validation commands for the Phase 1 surface.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-api-type-contracts/01-CONTEXT.md
@components/react/package.json
@apps/react/package.json
@apps/react-vite/package.json
@apps/nextjs/package.json
@apps/nextjs-beta/package.json
@templates/react/package.json
@templates/react-ts/package.json

<interfaces>
From `components/react/package.json`:
```json
"peerDependencies": {
  "@tsparticles/engine": "^4.0.0-beta.11",
  "react": ">=16.8.0 <20",
  "react-dom": ">=16.8.0 <20"
}
```

From consumer packages (`apps/*`, `templates/*`):

```json
"dependencies": {
  "@tsparticles/react": "workspace:^",
  "@tsparticles/engine": "..."
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align core package dependency policy to Phase 1 decisions</name>
  <files>components/react/package.json</files>
  <read_first>
    - components/react/package.json
    - .planning/phases/01-core-api-type-contracts/01-CONTEXT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>Update `components/react/package.json` dependency metadata to keep React peer compatibility broad (`>=16.8.0 <20`) per D-11 while explicitly targeting the active `@tsparticles/engine` 4.0.0 beta line per D-02. Keep entry-point semantics (`main`, `types`, publish files) intact for bundler compatibility per D-12. Any recommended alternative from research should be noted but overridden by locked decision D-02.</action>
  <acceptance_criteria>
    - `components/react/package.json` peerDependencies include `react` and `react-dom` as `>=16.8.0 <20`.
    - `components/react/package.json` peerDependencies include `@tsparticles/engine` on `^4.0.0-beta` line.
    - `main` and `types` fields remain present and unchanged in intent.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @tsparticles/react build:ci</automated>
  </verify>
  <done>Core package metadata reflects locked compatibility policy without API contract breakage.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate engine compatibility alignment to apps and templates</name>
  <files>apps/react/package.json, apps/react-vite/package.json, apps/nextjs/package.json, apps/nextjs-beta/package.json, templates/react/package.json, templates/react-ts/package.json</files>
  <read_first>
    - apps/react/package.json
    - apps/react-vite/package.json
    - apps/nextjs/package.json
    - apps/nextjs-beta/package.json
    - templates/react/package.json
    - templates/react-ts/package.json
    - components/react/package.json
    - .planning/phases/01-core-api-type-contracts/01-CONTEXT.md
  </read_first>
  <action>Align monorepo consumer dependency declarations with the core package target order (package first, consumers immediately after) per D-03. Ensure each app/template consuming `@tsparticles/react` references a compatible `@tsparticles/engine` beta major line with Phase 1 policy while preserving each project’s framework-specific dependencies and scripts unchanged.</action>
  <acceptance_criteria>
    - All listed app/template package files contain `@tsparticles/react` plus a compatible `@tsparticles/engine` declaration.
    - No removal of existing framework runtime dependencies (`react`, `next`, `react-scripts`, `vite`) from those manifests.
    - Dependency lines across library/apps/templates no longer conflict on engine major version.
  </acceptance_criteria>
  <verify>
    <automated>pnpm run build:ci</automated>
  </verify>
  <done>Library and immediate consumers share compatible dependency contracts and build together.</done>
</task>

<task type="auto">
  <name>Task 3: Run compatibility smoke validation across consumer matrix</name>
  <files>components/react/package.json, apps/react/package.json, apps/react-vite/package.json, apps/nextjs/package.json, apps/nextjs-beta/package.json, templates/react/package.json, templates/react-ts/package.json</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>Execute monorepo compatibility smoke checks immediately after dependency alignment (per D-03) to validate that contract and type updates from Plans 01/02 remain consumable in existing downstream packages. Use CI-oriented commands already present in the workspace to avoid ad-hoc scripts.</action>
  <acceptance_criteria>
    - `pnpm --filter @tsparticles/react build:ci` exits 0.
    - `pnpm run build:ci` exits 0 at workspace root.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @tsparticles/react build:ci && pnpm run build:ci</automated>
  </verify>
  <done>Phase 1 compatibility posture is validated by real consumer builds in the monorepo matrix.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                               | Description                                                           |
| ------------------------------------------------------ | --------------------------------------------------------------------- |
| package metadata → consumer install/runtime resolution | Dependency/version contracts influence what users install and execute |

## STRIDE Threat Register

| Threat ID | Category | Component                             | Disposition | Mitigation Plan                                                                                                             |
| --------- | -------- | ------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------- |
| T-01-07   | T        | components/react/package.json         | mitigate    | Keep explicit peer ranges for React and engine to prevent unintended contract tampering through implicit resolver behavior. |
| T-01-08   | D        | apps/_ + templates/_ package.json     | mitigate    | Align engine major lines across consumers to reduce install/build breakage from dependency skew.                            |
| T-01-09   | R        | workspace build verification commands | mitigate    | Require deterministic CI build commands after manifest updates to produce auditable success/failure evidence.               |

</threat_model>

<verification>
- `pnpm --filter @tsparticles/react build:ci`
- `pnpm run build:ci`
</verification>

<success_criteria>

- D-01/D-02/D-03 dependency policy is reflected in manifests.
- React compatibility guardrail (D-11) remains preserved.
- Bundler/package entry compatibility contract (D-12) remains intact and validated by workspace builds.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-api-type-contracts/01-core-api-type-contracts-03-SUMMARY.md`
</output>
