---
phase: 01-core-api-type-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/react/lib/IParticlesProps.ts
  - components/react/lib/index.ts
  - components/react/lib/ParticlesProvider.tsx
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - CORE-04
must_haves:
  truths:
    - "TypeScript consumers get explicit prop and callback types from @tsparticles/react exports."
    - "Consumers can call initParticlesEngine with typed Engine callback without custom type assertions."
  artifacts:
    - path: "components/react/lib/IParticlesProps.ts"
      provides: "Canonical typed props contract including options/url and typed callbacks"
    - path: "components/react/lib/index.ts"
      provides: "Stable public API barrel with Particles + initParticlesEngine exports"
    - path: "components/react/lib/ParticlesProvider.tsx"
      provides: "Provider/hook type contract aligned with engine initialization callback type"
  key_links:
    - from: "components/react/lib/index.ts"
      to: "components/react/lib/IParticlesProps.ts"
      via: "re-export"
      pattern: "export type \{ IParticlesProps \}"
    - from: "components/react/lib/index.ts"
      to: "@tsparticles/engine"
      via: "dynamic import in initParticlesEngine"
      pattern: "import\(\"@tsparticles/engine\"\)"
---

<objective>
Lock the Phase 1 public TypeScript contract so consumers keep a stable, additive API with stronger callback typings.

Purpose: Deliver D-04, D-06, D-07, and D-08 without breaking consumer imports.
Output: Updated contract files in `components/react/lib/*` that preserve the existing public surface while improving type safety.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-api-type-contracts/01-CONTEXT.md
@components/react/lib/IParticlesProps.ts
@components/react/lib/index.ts
@components/react/lib/ParticlesProvider.tsx

<interfaces>
From `components/react/lib/IParticlesProps.ts`:
```typescript
export interface IParticlesProps {
  id?: string;
  options?: ISourceOptions;
  url?: string;
  style?: CSSProperties;
  className?: string;
  particlesInit?: (engine: Engine) => Promise<void> | void;
  particlesLoaded?: (container?: Container) => Promise<void> | void;
}
```

From `components/react/lib/index.ts`:

```typescript
export async function initParticlesEngine(
  cb: (engine: Engine) => Promise<void>,
): Promise<void>;
export default Particles;
export { Particles };
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten exported prop and callback type contracts</name>
  <files>components/react/lib/IParticlesProps.ts, components/react/lib/ParticlesProvider.tsx</files>
  <read_first>
    - components/react/lib/IParticlesProps.ts
    - components/react/lib/ParticlesProvider.tsx
    - .planning/phases/01-core-api-type-contracts/01-CONTEXT.md
  </read_first>
  <behavior>
    - TS consumer can import and use `IParticlesProps` with typed `options: ISourceOptions` (TYPE-02).
    - `particlesInit` and `particlesLoaded` remain optional and typed without requiring `any` casts (TYPE-01).
  </behavior>
  <action>Update type definitions additively (per D-06) so `Engine`, `Container`, and `ISourceOptions` remain explicit in public contracts (per D-07). Preserve optional callback semantics while clarifying callback signatures for IntelliSense and compile-time checks (per D-08). Keep provider callback typing aligned with the same engine callback contract used by `initParticlesEngine` (per D-04).</action>
  <acceptance_criteria>
    - `components/react/lib/IParticlesProps.ts` contains `options?: ISourceOptions`.
    - `components/react/lib/IParticlesProps.ts` contains `particlesInit?: (engine: Engine)`.
    - `components/react/lib/IParticlesProps.ts` contains `particlesLoaded?: (container?: Container)`.
    - `components/react/lib/ParticlesProvider.tsx` callback prop type matches Engine-based async callback shape.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @tsparticles/react build:ci</automated>
  </verify>
  <done>Public type contracts compile cleanly and remain backward-compatible for existing consumer code paths.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve and validate stable public exports</name>
  <files>components/react/lib/index.ts</files>
  <read_first>
    - components/react/lib/index.ts
    - components/react/lib/IParticlesProps.ts
    - .planning/phases/01-core-api-type-contracts/01-CONTEXT.md
  </read_first>
  <action>Keep the package surface centered on `Particles` and `initParticlesEngine` exactly as the primary entrypoints (per D-04), with additive-only export adjustments if needed (per D-06). Ensure the barrel still exports `IParticlesProps`, `ParticlesProvider`, and `useParticlesEngine` using existing codebase export patterns (per D-10).</action>
  <acceptance_criteria>
    - `components/react/lib/index.ts` still has `export async function initParticlesEngine`.
    - `components/react/lib/index.ts` still has `export default Particles` and `export { Particles }`.
    - `components/react/lib/index.ts` exports `IParticlesProps`, `ParticlesProvider`, and `useParticlesEngine`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @tsparticles/react build:ci</automated>
  </verify>
  <done>Existing imports from `@tsparticles/react` remain valid while typed exports are explicit and stable.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                              |
| -------------------------------------- | ------------------------------------------------------------------------ |
| consumer TypeScript code → package API | Untrusted consumer code passes callbacks/options into exported contracts |

## STRIDE Threat Register

| Threat ID | Category | Component                                  | Disposition | Mitigation Plan                                                                                                           |
| --------- | -------- | ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-01-01   | T        | components/react/lib/IParticlesProps.ts    | mitigate    | Keep strict exported callback and options types to prevent contract tampering via `any`-like ambiguities at compile time. |
| T-01-02   | R        | components/react/lib/index.ts              | mitigate    | Preserve explicit named/default exports so consumer usage remains auditable and deterministic across builds.              |
| T-01-03   | D        | components/react/lib/ParticlesProvider.tsx | accept      | Provider typing itself is low DoS risk; runtime hardening is explicitly deferred to Phase 2 per CONTEXT deferred scope.   |

</threat_model>

<verification>
Run package-level compile/build validation:
- `pnpm --filter @tsparticles/react build:ci`
</verification>

<success_criteria>

- `TYPE-01` and `TYPE-02` contracts are explicitly represented in exported types.
- `CORE-04` helper entrypoint typing remains stable and callable.
- No breaking export removals were introduced.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-api-type-contracts/01-core-api-type-contracts-01-SUMMARY.md`
</output>
